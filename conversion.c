//pop off SLAYYYYYYY
#include <cs50.h>
#include <stdio.h>
#include <string.h>
#include <strings.h>

string htob(string a);
string dtoh(int a);
int btod(string b);

int main(void);
{
    string t = get_string("What data type are you starting with?");
    string s = get_string("What data type would you like to convert to?");
    if (strcasecmp(t, "hexadecimal") == 0)
    {
        string h = ("Please enter your hexadecimal here: )
    }
}
